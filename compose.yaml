services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        HOST_UID: ${HOST_UID:-1000}
        HOST_GID: ${HOST_GID:-1000}
        USER_NAME: builder
    working_dir: /workspace
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
